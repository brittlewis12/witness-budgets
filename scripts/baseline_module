#!/usr/bin/env bash
set -euo pipefail
if command -v uv >/dev/null 2>&1; then
  if uv run scripts/baseline_module.py "$@"; then
    exit 0
  else
    echo "uv run failed, falling back to python3" >&2
  fi
fi
exec python3 scripts/baseline_module.py "$@"
